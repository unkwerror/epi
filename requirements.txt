# Зависимости объявлены в pyproject.toml -- этот файл нужен только для того,
# чтобы поднять окружение разработки одной командой:
#
#   python -m venv .venv
#   ./.venv/bin/pip install -e ".[notebook]"
#
# Держать список пакетов ещё и здесь нельзя: две копии неизбежно разъедутся.

-e .[notebook]
